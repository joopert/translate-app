// still need this because vscode extension does not support typescript config
//https://github.com/prettier/prettier-vscode/issues/3623

const config = {
  trailingComma: "all",
  singleQuote: true,
  tabWidth: 2,
  printWidth: 100,
  bracketSpacing: true,
  arrowParens: "avoid",
  vueIndentScriptAndStyle: false,
  semi: true,
};

export default config;
