import { type Config } from "prettier";

const config: Config = {
    trailingComma: "all",
    singleQuote: true,
    tabWidth: 2,
    printWidth: 100,
    bracketSpacing: true,
    arrowParens: "avoid",
    vueIndentScriptAndStyle: true,
    semi: true,
};

export default config;
